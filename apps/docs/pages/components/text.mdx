import { LabelExample } from './../../src/examples/text/label'
import { DescriptionExample } from './../../src/examples/text/description'
import { TextErrorExample } from './../../src/examples/text/textError'
import { getLink } from './../../src/utils/routes'
import { NextSeo } from 'next-seo'

export const stylesLink = getLink('styles')

<NextSeo title="Text" />

# @pikas-ui/text

This package contains a text components.

## Getting Started

You need to install the <a href={stylesLink}>@pikas-ui/styles</a> package to use this package.

### Installation

With npm:

```
npm install @pikas-ui/text
```

With yarn:

```
yarn add @pikas-ui/text
```

With pnpm:

```
pnpm add @pikas-ui/text
```

---

## Usage

### Label

<LabelExample />

```tsx
import { Label } from `@pikas-ui/text`;

const Example: React.FC = () => {
  return <Label>Label</Label>
}
```

#### Props

| Prop       | Description             | Type              | Default |
| :--------- | :---------------------- | :---------------- | :------ |
| `htmlFor`  | The id of the label.    | `string`          | -       |
| `style`    | The style of the label. | `CSS`             | -       |
| `children` | The text of the label.  | `React.ReactNode` | -       |

---

### Description

<DescriptionExample />

```tsx
import { Description } from `@pikas-ui/text`;

const Example: React.FC = () => {
  return <Description>Description</Description>
}
```

#### Props

| Prop       | Description                   | Type              | Default |
| :--------- | :---------------------------- | :---------------- | :------ |
| `style`    | The style of the description. | `CSS`             | -       |
| `children` | The text of the description.  | `React.ReactNode` | -       |

---

### TextError

<TextErrorExample />

```tsx
import { TextError } from `@pikas-ui/text`;

const Example: React.FC = () => {
  return <TextError>TextError</TextError>
}
```

#### Props

| Prop       | Description                  | Type              | Default |
| :--------- | :--------------------------- | :---------------- | :------ |
| `style`    | The style of the text error. | `CSS`             | -       |
| `children` | The text of the text error.  | `React.ReactNode` | -       |
