import { NextSeo } from 'next-seo'

<NextSeo title="Styles" />

# @pikas-ui/styles

This package contains a styles parameters.

The library is based on [Stitches](https://stitches.dev/).

## Getting Started

---

### Installation

With npm:

```
npm install @pikas-ui/styles
```

With yarn:

```
yarn add @pikas-ui/styles
```

With pnpm:

```
pnpm add @pikas-ui/styles
```

---

## Usage

---

### globalCss

For handling global styles in your application you can use the `globalCss` function.
You must call it in your application.

```tsx
import { globalCss, createTheme } from '@pikas-ui/styles'

const globalCssCustom = globalCss({
  'html, body, #__next': {
    color: '$black',
    fontFamily: '$roboto',
    minWidth: 300,
    scrollBehavior: 'smooth',
  },
  '*': {
    margin: 0,
    padding: 0,
    boxSizing: 'border-box',
  },
  li: {
    listStyle: 'none',
  },
  a: {
    textDecoration: 'none',
  },

  '::-webkit-scrollbar': {
    width: '7px',
    height: '7px',
    borderRadius: '10px',
  },
  '::-webkit-scrollbar-track': {
    borderRadius: '10px',
    backgroundColor: '$white',
  },
  '::-webkit-scrollbar-thumb': {
    background: '$gray',
    borderRadius: '10px',
  },
})

const myTheme = createTheme({
  colors: {
    primary-darker: '#083358',
    primary-dark: '#0D4C84',
    primary: '#1166B0',
    primary-light: '#88B3D7',
    primary-lighter: '#C3D9EB',
    primary-lightest-2: '#E7F0F7',
    primary-lightest-1: '#F3F4F6',
  },
})

const App: React.FC = () => {
  globalCssCustom()

  return (
    <PikasUIProvider lightTheme={myTheme}>
      <h1>Hello World</h1>
    </PikasUIProvider>
  )
}
```

---

### Styling

```ts
import { styled } from '@pikas-ui/styles'

const Button = styled('button', {
  background: '$primary',
  color: 'white',
  border: 'none',
  padding: '0.5rem',
  borderRadius: '$sm',
  fontSize: '$em-small',
  fontWeight: '$bold',
  cursor: 'pointer',
  outline: 'none',
  transition: 'all 0.2s ease-in-out',

  '&:hover': {
    background: '$primary-light',
  },
})
```

## Themes

```tsx
import { createTheme } from '@pikas-ui/styles'

const themeDark = createTheme({
  colors: {
    primary-darker: '#083358',
    primary-dark: '#0D4C84',
    primary: '#1166B0',
    primary-light: '#88B3D7',
    primary-lighter: '#C3D9EB',
    primary-lightest-2: '#E7F0F7',
    primary-lightest-1: '#F3F4F6',
  },
})
```

---

## Theme Variables

---

### BorderRadius

Properties for border radius.

```bash
'none'
'xs'
'sm'
'md'
'lg'
'xl'
'round'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  borderRadius: '$sm',
})
```

---

### FontSize

Properties for font size.

```bash
'em-3x-small'
'em-2x-small'
'em-x-small'
'em-small'
'em-base'
'em-large'
'em-x-large'
'em-2x-large'
'em-3x-large'

'rem-3x-small'
'rem-2x-small'
'rem-x-small'
'rem-small'
'rem-base'
'rem-large'
'rem-x-large'
'rem-2x-large'
'rem-3x-large'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  fontSize: '$em-small',
})
```

---

### FontWeight

Properties for font weight.

```bash
'thin'
'extra-light'
'light'
'normal'
'medium'
'semi-bold'
'bold'
'extra-bold'
'black'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  fontWeight: '$bold',
})
```

---

### LetterSpacing

Properties for letter spacing.

```bash
'XX-SMALL'
'X-SMALL'
'SMALL'
'medium'
'LARGE'
'X-LARGE'
'XX-LARGE'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  letterSpacing: '$XX-SMALL',
})
```

---

### LineHeight

Properties for line height.

```bash
'em-3x-small'
'em-2x-small'
'em-x-small'
'em-small'
'em-base'
'em-large'
'em-x-large'
'em-2x-large'
'em-3x-large'

'rem-3x-small'
'rem-2x-small'
'rem-x-small'
'rem-small'
'rem-base'
'rem-large'
'rem-x-large'
'rem-2x-large'
'rem-3x-large'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  lineHeight: '$em-small',
})
```

---

### BoxShadow

Properties for box shadow.

```bash
'xs'
'sm'
'md'
'lg'
'xl'
'2xl'

'top-xs'
'top-sm'
'top-md'
'top-lg'
'top-xl'
'top-2xl'

'bottom-xs'
'bottom-sm'
'bottom-md'
'bottom-lg'
'bottom-xl'
'bottom-2xl'

'inner-sm'
'inner-md'
'inner-md'
'inner-lg'
'inner-xl'
'inner-2xl'

'inset-top-sm'
'inset-top-md'
'inset-top-md'
'inset-top-lg'
'inset-top-xl'
'inset-top-2xl'

'inset-bottom-sm'
'inset-bottom-md'
'inset-bottom-md'
'inset-bottom-lg'
'inset-bottom-xl'
'inset-bottom-2xl'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  boxShadow: '$xs',
})
```

---

### Size

Properties for size.

```bash
'XXX-SMALL'
'XX-SMALL'
'X-SMALL'
'SMALL'
'medium'
'LARGE'
'X-LARGE'
'XX-LARGE'
'XXX-LARGE'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  size: '$XXX-SMALL',
})
```

---

### ZIndeces

Properties for z-index.

```bash
'3x-low'
'2x-low'
'x-low'
'low'
'medium'
'high'
'x-high'
'2x-high'
'3x-high'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  zIndex: '$3x-low',
})
```

---

### Custom Gap

Properties for custom gap for resolve Safari gap issue.

```ts
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  gap: 8,
  // or
  columnGap: 8,
  // or
  rowGap: 8,
})
```

---

### Color

Properties for color.

#### Primary

```bash
'primary-darker'
'primary-dark'
'primary'
'primary-light'
'primary-lighter'
'primary-lightest-2'
'primary-lightest-1'
```

#### Secondary

```bash
'secondary-darker'
'secondary-dark'
'secondary'
'secondary-light'
'secondary-lighter'
'secondary-lightest-2'
'secondary-lightest-1'
```

#### Tertiary

```bash
'tertiary-darker'
'tertiary-dark'
'tertiary'
'tertiary-light'
'tertiary-lighter'
'tertiary-lightest-2'
'tertiary-lightest-1'
```

#### Black

```bash
'black'
'black-light'
'black-lighter'
'black-lightest-2'
'black-lightest-1'
```

#### Black fix

```bash
'black-fixed'
'black-fixed-light'
'black-fixed-lighter'
'black-fixed-lightest-2'
'black-fixed-lightest-1'
```

#### White

```bash
'white'
'white-light'
'white-lighter'
'white-lightest-2'
'white-lightest-1'
```

#### White fix

```bash
'white-fixed'
'white-fixed-light'
'white-fixed-lighter'
'white-fixed-lightest-2'
'white-fixed-lightest-1'
```

#### Gray

```bash
'gray-darker'
'gray-dark'
'gray'
'gray-light'
'gray-lighter'
'gray-lightest-2'
'gray-lightest-1'
```

#### Success

```bash
'success-darker'
'success-dark'
'success'
'success-light'
'success-lighter'
'success-lightest-2'
'success-lightest-1'
```

#### Warning

```bash
'warning-darker'
'warning-dark'
'warning'
'warning-light'
'warning-lighter'
'warning-lightest-2'
'warning-lightest-1'
```

#### Danger

```bash
'danger-darker'
'danger-dark'
'danger'
'danger-light'
'danger-lighter'
'danger-lightest-2'
'danger-lightest-1'
```

#### Other

```bash
'transparent' | 'background'
```

#### Example

```tsx
import { styled } from '@pikas-ui/styles'

const Div = styled('div', {
  color: '$primary',
  backgroundColor: '$background',
  borderColor: '$primary',
  borderWidth: 1,
  borderStyle: 'solid',
})
```

---

### Change Log

You can find the change log [here](https://github.com/Achaak/pikas/releases).

---

[Edit this page on GitHub](https://github.com/Achaak/pikas/blob/main/apps/docs-ui/pages/utilities/styles.mdx)
