# useTheme

import { NextSeo } from 'next-seo';

<NextSeo title="useTheme" />

This hook allows you to access the theme context and use the theme values.

The value change when the theme is changed.

## Requirements

To use this package, you must install the <a href={stylesLink}>@pikas-ui/styles</a> package.

## Installation

You can install this package using npm, yarn, or pnpm.

```
npm install @pikas-ui/styles
```

```
yarn add @pikas-ui/styles
```

```
pnpm add @pikas-ui/styles
```

---

## Usage

```tsx
import { useTheme } from '@pikas-ui/styles';

const Example: React.FC = () => {
  const useTheme = useTheme();

  return (
    <h1
      style={{
        color: useTheme.colors.primary,
      }}
    >
      Example
    </h1>
  );
};
```

---

## Contributing

This documentation can be edited on GitHub [here](https://github.com/Achaak/pikas/blob/main/apps/docs-ui/pages/utilities/useTheme.mdx)
