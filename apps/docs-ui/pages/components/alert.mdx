import { AlertExample } from './../../src/examples/alert/alert'
import { CustomAlertExample } from './../../src/examples/alert/customAlert'
import { docsUIRoutes } from '@pikas/docs-routes'
export const { getLink } = docsUIRoutes
import { NextSeo } from 'next-seo'

export const stylesLink = getLink('styles')

<NextSeo title="Alert" />

# @pikas-ui/alert

This package contains a alert component.

This library is based on [Radix Ui](https://www.radix-ui.com/).

<a href="https://github.com/Achaak/pikas/blob/main/packages/ui/alert/CHANGELOG.md">
  Change Log
</a>

## Getting Started

You need to install the <a href={stylesLink}>@pikas-ui/styles</a> package to use this package.

### Installation

With npm:

```
npm install @pikas-ui/alert
```

With yarn:

```
yarn add @pikas-ui/alert
```

With pnpm:

```
pnpm add @pikas-ui/alert
```

---

## Usage

### Alert

<AlertExample />

```tsx
import { Alert } from `@pikas-ui/alert`;

const Example: React.FC = () => {
  return (
    <Alert variant="alert">
      Ea Lorem fugiat aliquip irure aliquip quis mollit ullamco aliquip occaecat culpa.
    </Alert>
  )
}
```

#### With a custom configuration

```tsx
import { Alert } from `@pikas-ui/alert`;
import type {
  PikasCSS,
  FontSize,
  FontWeight
} from 'your_config'

const Example: React.FC = () => {
  return (
    <Alert<PikasCSS, FontSize, FontWeight> variant="alert">
      Ea Lorem fugiat aliquip irure aliquip quis mollit ullamco aliquip occaecat culpa.
    </Alert>
  )
}
```

#### Props

| Prop           | Description                          | Type              | Default      |
| :------------- | :----------------------------------- | :---------------- | :----------- |
| `variant`      | Alert variant of the alert component | `AlertVariant`    | `"info"`     |
| `children`     | Alert content                        | `ReactNode`       | -            |
| `fontSize`     | Font size of the alert component     | `PikasFontSize`   | `"EM-SMALL"` |
| `fontWeight`   | Font weight of the alert component   | `PikasFontWeight` | `"NORMAL"`   |
| `borderRadius` | Border radius of the alert component | `BorderRadius`    | `"md"`       |
| `iconSize`     | Icon size of the alert component     | `number`          | `24`         |
| `padding`      | Padding of the alert component       | `AlertPadding`    | `"md"`       |
| `gap`          | Gap of the alert component           | `AlertGap`        | `"sm"`       |
| `visible`      | Visibility of the alert component    | `boolean`         | `true`       |
| `css`          | Custom css of the alert component    | `AlertCSS`        | -            |

---

### CustomAlert

<CustomAlertExample />

```tsx
import { CustomAlert } from `@pikas-ui/alert`;
import type { IconProps } from '@pikas-ui/icons'

const IconExample: React.FC<IconProps> = (props) => {
  return <IconByName {...props} name="bx:baguette" />
}

const Example: React.FC = () => {
  return (
    <CustomAlert
      Icon={IconExample}
      color="WHITE"
      backgroundColor="SECONDARY"
    >
      This is an error alert.
    </CustomAlert>
  )
}
```

#### With a custom configuration

```tsx
import { CustomAlert } from `@pikas-ui/alert`;
import type { IconProps } from '@pikas-ui/icons'
import { IconByName } from '@pikas-ui/icons'
import type {
  PikasCSS,
  Color,
  FontSize,
  FontWeight
} from 'your_config'

const IconExample: React.FC<IconProps> = (props) => {
  return <IconByName {...props} name="bx:baguette" />
}

const Example: React.FC = () => {
  return (
    <CustomAlert<PikasCSS, Color, FontSize, FontWeight>
      Icon={IconExample}
      color="WHITE"
      backgroundColor="SECONDARY"
    >
      This is an error alert.
    </CustomAlert>
  )
}
```

#### Props

| Prop              | Description                             | Type              | Default      |
| :---------------- | :-------------------------------------- | :---------------- | :----------- |
| `children`        | Alert content                           | `ReactNode`       | -            |
| `fontSize`        | Font size of the alert component        | `PikasFontSize`   | `"EM-SMALL"` |
| `fontWeight`      | Font weight of the alert component      | `PikasFontWeight` | `"NORMAL"`   |
| `borderRadius`    | Border radius of the alert component    | `BorderRadius`    | `"md"`       |
| `iconSize`        | Icon size of the alert component        | `number`          | `24`         |
| `padding`         | Padding of the alert component          | `AlertPadding`    | `"md"`       |
| `gap`             | Gap of the alert component              | `AlertGap`        | `"sm"`       |
| `Icon`            | Icon of the alert component             | `FC<IconProps>`   | -            |
| `backgroundColor` | Background color of the alert component | `Colors`          | -            |
| `color`           | Color of the alert component            | `Colors`          | -            |
| `visible`         | Visibility of the alert component       | `boolean`         | `true`       |
| `css`             | Custom css of the alert component       | `AlertCSS`        | -            |
