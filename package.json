{
  "name": "@pikas-ui/monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "clean": "turbo run clean && rm -rf node_modules",
    "dev": "turbo run dev --parallel",
    "dev:storybook": "turbo run dev --filter=@pikas-ui/storybook...",
    "start": "turbo run start --parallel",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "pre-lint": "turbo run pre-lint",
    "changeset": "changeset",
    "changeset:version": "changeset version -r",
    "changeset:release": "turbo run build --filter=./packages/* && changeset publish"
  },
  "devDependencies": {
    "prettier": "latest",
    "turbo": "latest"
  },
  "engines": {
    "npm": ">=7.0.0",
    "node": ">=14.0.0"
  },
  "packageManager": "pnpm@7.0.0",
  "dependencies": {
    "@changesets/cli": "^2.22.0"
  }
}
